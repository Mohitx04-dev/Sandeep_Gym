import React from 'react'
import {Alert} from 'react-bootstrap'
function Landing() {
    return (
        <Alert variant='danger'>
        Restricted Access
        </Alert>
    )
}

export default Landing
